pipeline {
    agent  any
    stages {
        stage('build') {
            steps {
                ansiColor('gnome-terminal') {
                    sh 'export TRAVIS=1 ; ./make.sh -e "-j 2"'
                }
            }
        }
    }
}
